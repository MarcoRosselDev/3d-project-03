import React from "react";
import { GeneralContainer, Wrapper } from "./Menu.elements";

const Menu = () => {
  return (
    <GeneralContainer>
      <Wrapper></Wrapper>
    </GeneralContainer>
  );
};

export default Menu;
